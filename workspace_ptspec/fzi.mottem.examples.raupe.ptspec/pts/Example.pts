include "Target.pts";
include "Helper.pts";

test AutoDriving : Target default Core0
{
	run until main;
	
	Helper.CC_BackgroundUpdate(ref current_distance);
	
	run;
}

test ManualDriving : Target default Core0
{
	run until main;

	speed_override_active = 1;
	
	Helper.CC_BackgroundUpdate(ref current_distance);
	
	run;
}
